<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="Appointments.Views.SignUp" BackgroundColor="WhiteSmoke">
    
    <StackLayout Margin="0" VerticalOptions="StartAndExpand">
            <StackLayout>

                    <Label Text="Full Name" FontSize="Large"/>
            <Entry Text="{Binding User.FullName}" />
                    <Label Text="Gender" FontSize="Large" />
            <Entry Text="{Binding User.Gender}" />
                    <Label Text="Username" FontSize="Large" />
            <Entry Text="{Binding User.Username}" />
                    <Label Text="Password" FontSize="Large" />
            <Entry Text="{Binding User.Password}" />
                    <Label Text="Re-Type Password" FontSize="Large" />
            <Entry Text="{Binding User.ConfirmPassword}"/>
            </StackLayout>
            <StackLayout VerticalOptions="Center" HorizontalOptions="Center"></StackLayout>
        <Button Text="SignUp" Command="{Binding ToSignUp}" CornerRadius="30" BackgroundColor="Gray"/>
        </StackLayout>
    
</ContentPage>
    
    